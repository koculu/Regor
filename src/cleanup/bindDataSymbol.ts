/**
 * Symbol used to attach Regor's internal `BindData` object to DOM nodes.
 * The `BindData` stores information required for unbinding and cleanup.
 */
export const bindDataSymbol = Symbol(':regor')
