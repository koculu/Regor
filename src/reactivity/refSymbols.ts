/** Symbol attached to objects created by {@link ref}. */
export const refSymbol = Symbol('ref')
/**
 * Symbol attached to objects created by {@link sref} and computed refs.
 */
export const srefSymbol = Symbol('sref')
export const rawSymbol = Symbol('raw')
